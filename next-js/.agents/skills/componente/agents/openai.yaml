interface:
  display_name: "Componente"
  short_description: "Cria componentes reutilizaveis no next-js"
  default_prompt: "Use $componente para criar ou refatorar um componente no next-js."
policy:
  allow_implicit_invocation: true
