# URL do backend NestJS (server-side apenas — nunca prefixar com NEXT_PUBLIC_,
# ver next-js/docs/ai/SECURITY.md). Local: http://localhost:3000.
# Producao: https://api.drs.fasters.com.br
BACKEND_API_URL=http://localhost:3000
