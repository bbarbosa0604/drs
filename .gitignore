# Dependencias
node_modules/
.pnp/
.pnp.js
.yarn/install-state.gz

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# Sistema operacional
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Segredos — nunca versionar valor real; .env.example e o unico .env versionado
.env
.env.*
!.env.example

# Build e output
dist/
build/
out/
coverage/
.next/
.turbo/
.expo/
*.tsbuildinfo
next-env.d.ts

# Certificado local (ex.: HTTPS em dev)
*.pem

# Documentos gerados (Task 026) - storage local de dev, nunca versionar
backend/storage/

# Deploy
.vercel

# Saida de QA visual (screenshot/relatorio regerado a cada rodada) — nome de pasta comum
# entre stacks; cada stack decide se usa
.visual-out/

# projetos de produto: cada um e repositorio git independente
projetos/
