# Backend

NestJS backend scaffold with modular architecture, JWT security, validation, events and agent-first documentation.

## Commands

- `npm install`
- `npm run start:dev`
- `npm run build`
- `npm run lint`
- `npm run test`
