/** Classificacao de escopo (PRD secao 12) — usada por Cadeia de Valor, Topologia e Arquitetura. */
export enum ScopeClassification {
  IN_SCOPE = 'IN_SCOPE',
  OUT_SCOPE = 'OUT_SCOPE',
  INTERFACE = 'INTERFACE',
}
